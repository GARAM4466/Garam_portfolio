# GitHub repo used as the data store (Git-as-CMS).
# Create a fine-grained Personal Access Token with Contents: Read & Write on this repo only.
GITHUB_TOKEN="github_pat_xxx"
GITHUB_OWNER="GARAM4466"
GITHUB_REPO="Garam_portfolio"
GITHUB_BRANCH="main"

# Admin login password (used on the site's Admin login screen).
ADMIN_PASSWORD="choose-a-strong-password"

# Secret used to sign admin session tokens. Use a long random string.
AUTH_SECRET="long-random-string-change-me"
